import 'package:flutter/material.dart';



class Onc extends StatelessWidget {
  const Onc({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('onc'),
        ),
      body:const Text('once'),

      

    );
  }
}